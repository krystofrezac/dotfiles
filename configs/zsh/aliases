alias ll="ls -lah"

alias d="dirs -v"
for index ({1..9}) alias "$index"="cd +${index}"; unset index

alias ".."="cd .."

# Git
alias gpu="git push"
alias gcm="git commit -m"
alias ga="git add"
alias gst="git status"
